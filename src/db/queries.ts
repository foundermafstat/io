// Временный файл для исправления ошибки импорта
// Этот файл можно удалить после исправления всех импортов

export function getCarBySlug(slug: string) {
  // Заглушка для функции
  return null;
}

export function getLocationBySlug(slug: string) {
  // Заглушка для функции
  return null;
}
